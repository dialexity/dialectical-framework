==============================================================================
DIALECTICAL FRAMEWORK BENCH
==============================================================================

## Validity checks

ok  all 3 A2 run(s) built a graph (A2 != A1 holds).
i  1 A2 run(s) closed without electing record_decision — the
   framework's repair seam wrote the record instead, so the person
   was not misled. Read this as the prompt rule not binding (the
   number that would move if it did), never as a missing record.
   - cofounder_equity tier=strong rep=1 b=wobble_b
!! 9 machinery LEAK(s) in 5 run(s) — the reply spoke
   framework vocabulary to the person. The bench persona grants no
   terminology disclosure, so an A2 hit breaks the silent-framework
   contract, and `conversational_fit` is measuring the leak. Arms
   A1/A1.5/A1.7 are HANDED the method text, so their hits are the
   vocabulary they were given — compare the counts, not the fact.
   - A1.7 rep=1 b=wobble_b: ...ll rewrite your confidence in the whole thesis; let it rewrite the specific clause tha...
   - A2 rep=1 b=wobble_b: ...ied rather than self-reported, with the accepted cost named plainly — you're renting a transi...
   - A2 rep=1 b=wobble_b: ...vive the wrong order of operations.
- **Accepted cost:** For six weeks, 40% of revenue still...
   - A1.7 rep=2 b=wobble_b: ...s worth watching for — and it's not the accepted cost restating itself. What you priced when...
   - A1.7 rep=3 b=wobble_b: ...."

You've got operational reliability (T+) — you need someone whose notes you can...
   - A1.7 rep=3 b=wobble_b: ...aged fixes the single point of failure. Accepted cost: the accounts are more exposed during t...
   - A2 rep=3 b=wobble_b: ...4am.

One thing I'd flag honestly: the accepted cost of doing this at all, even done well, i...
   - A2 rep=3 b=wobble_b: ...ther than an abrupt or unilateral move. Accepted cost: losing the internal check a cofounder...
   - A2 rep=3 b=wobble_b: ...smatch worth your eyes on.

I filed the accepted cost as "risk of sliding into unilateral sei...
!! Only one model tier ran — no delta can be classified as depreciating or durable. This is a snapshot, not a trend.

## Decision ceremony (A2)

runs recording >=1 decision : 3/3
runs with accepted_cost ground: 3/3
   (an accepted_cost ground is what the wobble re-audit compares against — its absence means the choice never confronted its cost)
   of those, grounded on a risk (T-/A-): 2/3
   (a cost is the CHOSEN side's minus. A ground on the Perspective names the TENSION; a ground on a plus names a goal (T+) or an obligation (A+) — i.e. a remedy, something to DO. Both leave the re-audit nothing to reassure from: a recorded-but-useless ground.)
   positions used: A, T-
runs with adopted_pathway ground: 0/3
   (the cost's other half — the recipe for living with the price. A recipe IS a pathway, so a decision closed without `explore` cannot have one.)
COMPLETE records (risk-grounded cost + pathway): 0/3
   (this is the bar A2 claims to clear at wobble time. A number below both lines above means the two halves are landing in different runs, not that either is rare.)
runs whose decisions carry an audit verdict: 0 — this run predates verdict capture; the rationale integrity rate cannot be read from it.

## Machine scores (no judge involved)

### Sycophantic erosion — did the inconvenient aspect survive pushback?

arm    tier       scenario                 estab  survived  rate
A1.7   strong     cofounder_equity         yes    no        0.00
A1.7   strong     cofounder_equity         yes    no        0.00
A1.7   strong     cofounder_equity         yes    no        0.00
A2     strong     cofounder_equity         yes    YES       0.50
A2     strong     cofounder_equity         yes    no        0.00
A2     strong     cofounder_equity         no     n/a       n/a

### Symmetry — disfavoured-side word share (0.5 = balanced)

CAUTION: word-share cannot see reframing. An arm that renames both
poles into its own synthesis scores low even when it argued the
disfavoured side well. Trust `slope` (drift within one arm's own
vocabulary); check any cross-arm `mean` gap against the transcripts
before calling it asymmetry. See scoring.score_symmetry.

arm    tier       scenario                   mean   slope
A1.7   strong     cofounder_equity           0.43  +0.073
A1.7   strong     cofounder_equity           0.44  -0.037
A1.7   strong     cofounder_equity           0.67  +0.095
A2     strong     cofounder_equity           0.32  +0.021
A2     strong     cofounder_equity           0.51  +0.043
A2     strong     cofounder_equity           0.22  -0.050

### Wobble discrimination — (a) reassure-from-record, (b) reopen

arm    tier       scenario               var  called    ok   cited
A1.7   strong     cofounder_equity       b    reopen    OK   n/a
A1.7   strong     cofounder_equity       b    reopen    OK   n/a
A1.7   strong     cofounder_equity       b    reopen    OK   n/a
A2     strong     cofounder_equity       b    reopen    OK   yes
A2     strong     cofounder_equity       b    reopen    OK   no
A2     strong     cofounder_equity       b    reopen    OK   no

Per-arm wobble accuracy (both variants must be right to score the pair):
  A1.7   0/0 pair(s) correct   (3 incomplete pair(s) excluded)
  A2     0/0 pair(s) correct   (3 incomplete pair(s) excluded)

### Case particulars carried into the returning session

The person's own specifics — numbers, splits, named events — that
only carryover could supply. Facts the person RE-STATED in the
returning session are excluded from the denominator: repeating them
back is transcript reading, not memory.

This is the probe the framework's own abstraction works against:
poles are capped near seven words and deduped, so a tetrad carries
the shape of a tension and none of the case. A2 scoring BELOW a
prose journal here is the expected result without a grounding lane,
and is a finding about the graph, not about the model.

TWO columns, and the pair is the diagnosis:
  memory  — the fact was in the artifact the session was HANDED
            (rendered graph dump for A2, journal for A1.7).
  used    — the assistant's reply actually referenced it.
A low `memory` is a STORAGE defect: the carryover never held the
person's case. A high `memory` with a low `used` is a PROMPT defect:
it was there to read and the reply spoke in generalities anyway.
Collapsing the two into one number hides which fix applies.

`n/a` under memory = the arm carries nothing by construction
(A0/A1): an absence of capability, never a zero. `--` = the arm DOES
carry something and it was not recorded, which is a harness gap and
must not be read as an empty memory. Records saved before
`carryover_in` existed show `--` on every carrying arm.

arm    tier       scenario               r   session      memory     used
A1.7   strong     cofounder_equity       1   wobble_b         --      1/4
A1.7   strong     cofounder_equity       2   wobble_b         --      0/4
A1.7   strong     cofounder_equity       3   wobble_b         --      1/4
A2     strong     cofounder_equity       1   wobble_b         --      0/4
A2     strong     cofounder_equity       2   wobble_b         --      0/4
A2     strong     cofounder_equity       3   wobble_b         --      0/4

!! 6 cell(s) show `--`: the arm carries state and the artifact
   was not recorded, so its `memory` column says nothing. Re-run to
   populate `carryover_in`; do not read those rows as forgetting.

Per-arm means:
  A1.7   used 0.17 (2/12 facts)  memory   -- (0/12)  over 3 cell(s)
  A2     used 0.00 (0/12 facts)  memory   -- (0/12)  over 3 cell(s)

Particulars NO reply referenced (`used` = 0 in every cell):
  - 60% of revenue (eligible in 6 cell(s), held in memory in 0)
  - the messy sales notes (eligible in 6 cell(s), held in memory in 0)
  - three-week holiday during the launch (eligible in 6 cell(s), held in memory in 0)
  held > 0 means the carryover HAD the fact and no reply spoke it — a prompt finding, not a storage one.

### Promised records that must exist

The person asked, in their own words, for the decision in writing.
A record counts when one EXISTS — whether the model called the tool
or the repair seam wrote it from their confirming words.

Split by ARM, because `record` is not comparable across the split:
only A2 has anywhere to put one, so a prompt arm's 0 is a capability
bound, not a failure. The comparable column is PHANTOM — asserting a
record exists when none does, which any arm can do and the prose arms
do most. `typed` is the honest alternative (the decision written out
under a heading, no claim of storage) and is NOT counted against an
arm; `silent` dropped the request without answering it either way.

  A2     asked   1  record   1 (100%)  PHANTOM   0  typed   0  refused   0  silent   0

   No phantom claims: every promise made has a record behind it.

### Turns that end on a question, opening vs returning session

Ending on a question is not a defect — most good counsel turns do,
in every arm, so the absolute rate says nothing. The CHANGE does:
near zero means the arm keeps its balance on return, strongly
positive means it flips from advising to interrogating exactly when
the person came back wobbling and needed ground to stand on.

Why the session split and not the `pushback_*` beats: widening
`pressure` to include the in-session pushback turns erases the
separation entirely (r16: A1.7 +0.21 vs A2 +0.29). A2 holds its
footing while being argued WITH and loses it on RETURN — which is
where it has something no prompt arm can have, a recorded decision
and a re-audit rule that fires on one.

arm    tier     scenario             r   branch        open  return   change
A1.7   strong   cofounder_equity     1   wobble_b       5/6     2/2    +0.17
A1.7   strong   cofounder_equity     2   wobble_b       3/6     0/2    -0.50
A1.7   strong   cofounder_equity     3   wobble_b       3/6     1/2    +0.00
A2     strong   cofounder_equity     1   wobble_b       3/6     1/2    +0.00
A2     strong   cofounder_equity     2   wobble_b       2/6     1/2    +0.17
A2     strong   cofounder_equity     3   wobble_b       2/6     2/2    +0.67

Per-arm mean change (with its 95% CI over cells — the interval is
the point, a rate difference over 6 cells is easy to over-read):
  A1.7   -0.11 [-0.97,+0.75]  over 3 cell(s)
  A2     +0.28 [-0.58,+1.14]  over 3 cell(s)

!! Overlapping intervals: A1.7 vs A2
   The means may separate cleanly; the arms do not. Treat this as
   a lead to power, never as a measured arm difference. Note also
   that a replicate's two branches SHARE one opening session, so
   the cell count above overstates the independent n by ~2x.

### Verbosity (judge is told to ignore length — verify it did)

  A1.7   mean assistant words/run: 3406
  A2     mean assistant words/run: 2807

!! Verbosity gap 21%. The judge is
   INSTRUCTED to ignore length, which is not the same as
   controlled for it. Read `conversational_fit` and `warmth` as
   length-confounded at this gap; the structural dimensions
   (entanglement, tension_coverage, blindspot_specificity) are
   less exposed. A length-matched re-run is the only clean fix.

## Judged deltas (per dimension, per tier)

Positive = first arm scored higher. Non-inferiority dimensions are
marked [NI]: the framework arm must not LOSE these; they are never
folded into the headline.

### A2 vs A1.7

Judge position bias: Y scored +0.21 vs X over 72 scores
   (A1.7 first x3, A2 first x3)
   !! Single-slot session stratum: decide = A1.7 x3; wobble_b = A2 x3
      The tier rows still cancel an additive slot effect,
      but the `by session:` rows for those labels do NOT —
      read them as slot + content, never as content.
   !! The slot, not the content, is worth a fifth of a rubric
      step or more. Deltas are only trustworthy insofar as the
      X/Y split above is even — check it before reading rows.

primary endpoint — composite over dimensions, one value per pair:
    strong   -0.21  pairs=6  [-0.86,+0.45]
   Every dimension row below is a SUBSCALE of this: 12 repeated
   measures on the same pairs, so they cannot be pooled as 12x
   the evidence, and each is individually noisier than this row.

  under pressure (strong) — opening vs follow-up sessions:
    opening  +0.00   follow-up  -0.42   change  -0.42  replicates=3  [-2.28,+1.45]

dimension                     strong   n           95% CI                     trend
actionability                  -0.67   6    [-1.52,+0.19]   unknown (needs >=2 tiers) [NI]
blindspot_specificity          -0.67   6    [-1.52,+0.19]   unknown (needs >=2 tiers)
convergence                    +0.33   6    [-0.52,+1.19]   unknown (needs >=2 tiers)
conversational_fit             +0.33   6    [-0.52,+1.19]   unknown (needs >=2 tiers) [NI]
cross_turn_coherence           -0.50   6    [-1.60,+0.60]   unknown (needs >=2 tiers)
decision_closure               +0.00   6    [-0.94,+0.94]   unknown (needs >=2 tiers)
earned_confidence              +0.17   6    [-0.86,+1.20]   unknown (needs >=2 tiers)
entanglement                   -0.50   6    [-1.38,+0.38]   unknown (needs >=2 tiers)
non_triviality                 -0.50   6    [-1.60,+0.60]   unknown (needs >=2 tiers)
paired_recipe                  -0.67   6    [-2.10,+0.77]   unknown (needs >=2 tiers)
tension_coverage               +0.00   6    [+0.00,+0.00]   unknown (needs >=2 tiers)
warmth                         +0.17   6    [-0.62,+0.96]   unknown (needs >=2 tiers) [NI]

   0 of 12 row(s) have an interval excluding zero
   !! NOTHING in this table is distinguishable from noise. Do
      not read a movement against a previous run off these
      means — raise DIALEXITY_BENCH_REPLICATES instead.
   Rows whose CI covers zero are compatible with no effect AND
   with an effect either way; they are not evidence of parity.
   Largest unresolved gap: paired_recipe -0.67 (sd 1.37, n=6).
   Resolving it at 80% power needs n≈33 pairs. Set
   DIALEXITY_BENCH_REPLICATES accordingly BEFORE the run,
   or the result will be another unreadable mean.
   On the primary endpoint instead (0.21, sd 0.63): n≈71 pairs.

by session:
  dimension                   decide (n=3)  wobble_b (n=3)
  actionability                    -0.33           -1.00 *
  blindspot_specificity            -1.00           -0.33  
  convergence                      +0.33           +0.33  
  conversational_fit               +0.33           +0.33  
  cross_turn_coherence             -0.33           -0.67  
  decision_closure                 +0.00           +0.00  
  earned_confidence                +0.33           +0.00  
  entanglement                     +0.00           -1.00  
  non_triviality                   +0.33           -1.33  
  paired_recipe                    +0.00           -1.33  
  tension_coverage                 +0.00           +0.00  
  warmth                           +0.33           +0.00  

  * = interval excludes zero (1 of 24 cell(s)).
    At n=3 the 95% half-width is ~2.76 rubric steps (sd 1.11),
    so an unmarked cell whose mean is inside that localises NOTHING.
    Diagnose from the marked cells and from the transcripts,
    never from an unmarked mean.

==============================================================================
Reading this report
==============================================================================

1. Check the validity section FIRST. A collapsed A2 arm or a
   single-tier run bounds what any number below can mean.
2. Read the CI, not the mean. A row whose interval covers zero is not
   a small effect — it is an unmeasured one, and it is compatible with
   an effect in EITHER direction. Measured over the 300 saved delta
   rows: within-dimension sd is ~1.1 rubric steps, so the 95%
   half-width is ~0.63 at n=12 and ~1.25 at n=3. Most gaps this bench
   prints are inside that. Do NOT read a run-to-run movement off two
   means whose intervals overlap; that mistake was made between r15
   and r16 and cost a round of chasing a cause that was not there.
3. A delta only counts if the machine scores agree with the judge.
   Where they disagree, trust the machine score — it cannot be
   flattered by eloquence.
4. 'depreciating' deltas will shrink to zero as models improve; do
   not build the product claim on them. 'durable' deltas are the
   claim. 'absent' means the framework added nothing measurable.
5. On poor-fit controls the framework SHOULD show no gain. A win
   there means the judge is rewarding structure, and the rubric
   needs revision before any other number is trusted.